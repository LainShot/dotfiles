#just the script to set up git stuff
git config --global user.email "19312376+LainShot@users.noreply.github.com"
git config --global user.name "LainShot"
git config --global user.email
git config --global user.name
echo "DONE EMAIL AND NAME GIT CONFIG SETUP"

